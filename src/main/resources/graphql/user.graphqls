type User {
    id: ID!
    email: String!
    userType: UserType!
    isActive: Boolean!
    createdAt: String
    updatedAt: String
    deletedAt: String
}

enum UserType {
    ADMIN
    PATIENT
    DOCTOR
    NURSE
}
